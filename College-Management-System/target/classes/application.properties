# Server Port Configuration
server.port=8080

# Spring DataSource Configuration for MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/college_management_system
spring.datasource.username=root
spring.datasource.password=@Carlitos0517
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate / JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true

# H2 Console Configuration (if using H2 for development)
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# Spring Security (Example)
spring.security.user.name=admin
spring.security.user.password=admin123
spring.security.user.roles=USER,ADMIN

# Logging Configuration
logging.level.org.springframework=INFO
logging.level.com.jccjr=DEBUG

# Thymeleaf or other template engine configuration (if you're using them)
# spring.thymeleaf.cache=false

# To enable debugging logs for Spring Boot auto-configuration
# debug=true

# Server Time Zone (optional)
spring.datasource.hikari.connection-timeout=30000
